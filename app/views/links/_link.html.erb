<%= f.error_messages %>
<ul class="form_list">
  <li>  
    <%= f.label :url, "url" %>  
    <%= f.text_field :url %>  
  </li>  
  
	<li>
	<%= f.label :category, "category" %>
	<%= f.collection_select(:category_id, current_user.categories, :id, :name, options = {:prompt => "select a category"}) %>
	</li>
	
	<li>
	<%= f.label :notes, "notes (optional)" %><br/>
	<%= f.text_area :notes, :cols => 80, :rows => 5 %>
	</li>
  
  <li>  
    <%= f.submit "submit" %>  
  </li>  
</ul>